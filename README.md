# ROS2 Launch 

* **`arg_test.launch.py`**
    * **주요 학습 내용**: Launch Arguments 사용법 학습. `DeclareLaunchArgument`를 이용해 외부에서 값을 받아와 Launch 파일 내에서 사용하는 방법을 시연합니다. 
* **`param_test.launch.py`**
    * **주요 학습 내용**: Node Parameters 설정 학습. `Parameter` 액션을 사용하거나 노드를 선언할 때 파라미터 값을 직접 설정하여 노드의 동작을 변경하는 방법을 시연합니다.
* **`turtle_color.launch.py`**
    * **주요 학습 내용**: `turtlesim` 노드의 파라미터 설정 및 변경. `turtlesim_node`를 실행하고, 파라미터를 설정하여 시뮬레이터 배경색을 변경하는 방법을 보여줍니다.
* **`turtle_sim.launch.py`**
    * **주요 학습 내용**: 간단한 노드 실행 및 Launch 파일 구조. `turtlesim` 시뮬레이터와 `turtle_teleop` 노드를 동시에 실행하는 기본적인 Launch 파일 구조를 보여줍니다.
* **`twin_turtles.launch.py`**
    * **주요 학습 내용**: 노드 이름 및 네임스페이스 재매핑. 동일한 노드 타입(`turtlesim_node`)의 인스턴스를 두 개 실행하고, 각기 다른 `node_name`과 `namespace`를 부여하여 충돌 없이 동작하도록 구성합니다.
* **`waffle_pi.launch.py`**
    * **주요 학습 내용**: (추정) 실제 로봇 환경 시뮬레이션 구성. `turtlebot3_gazebo`와 같은 실제 로봇(예: Waffle Pi) 시뮬레이션 환경을 구동하기 위한 복합적인 노드 그룹 및 설정 예시입니다.
* **`yaml_load.launch.py`**
    * **주요 학습 내용**: YAML 파일로부터 파라미터 로드. 외부 `.yaml` 파일에 정의된 파라미터들을 `LaunchConfiguration`과 함께 사용하여 노드에 일괄적으로 적용하는 방법을 시연합니다.

---

### 🐍 Python 노드

ROS 2의 Python 클라이언트 라이브러리(`rclpy`)를 사용하여 토픽 통신과 파라미터 처리를 수행하는 노드 예제입니다. 

* **`circle_turtle.py`**
    * **주요 학습 내용**: 토픽 발행 (Publisher) 노드. `turtlesim`의 `/turtle1/cmd_vel` 토픽에 지속적으로 속도 명령을 발행하여 거북이가 원을 그리도록 만드는 예제입니다.
* **`param_node.py`**
    * **주요 학습 내용**: 파라미터 처리 노드. 노드 내부에서 파라미터를 선언(`declare_parameter`)하고, 이를 읽거나 설정(`set_parameters`)하며, 파라미터가 변경될 때 콜백으로 처리하는 방법을 학습합니다.

---

### 💡 사용 방법

1.  **패키지 생성 및 파일 배치**: 이 파일들을 ROS 2 패키지 내의 적절한 위치(예: Launch 파일은 `launch/` 디렉토리, Python 노드는 `src/` 디렉토리)에 배치합니다.
2.  **빌드**: 패키지의 `setup.py` 파일과 `package.xml`을 올바르게 설정한 후 `colcon build`를 사용하여 빌드합니다.
3.  **실행**: Launch 파일을 실행합니다.
    ```bash
    ros2 launch <패키지_이름> <Launch_파일명>
    ```
    (예: `ros2 launch my_package arg_test.launch.py`)
